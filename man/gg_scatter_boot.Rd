% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_hist_qq_boot.R
\name{gg_scatter_boot}
\alias{gg_scatter_boot}
\title{Scatterplot Matrix of Bootstrap Estimates (ggplot2/GGally)}
\usage{
gg_scatter_boot(
  object,
  params,
  standardized = NULL,
  title = "Bootstrap Estimates",
  point_size = 1.8,
  point_alpha = 0.35,
  point_color = "#5DADE233",
  show_smooth = TRUE,
  smooth_method = "lm",
  smooth_se = FALSE,
  show_ellipse = TRUE,
  ellipse_level = 0.95,
  ellipse_color = "#8B0000CC",
  diag_type = c("both", "density", "hist", "blank"),
  hist_border_size = 0.2,
  bins = 30,
  dens_fill = "#5DADE233",
  dens_color = "#8B0000CC",
  hist_fill = "#5DADE233",
  hist_color = "#1B4F72",
  show_corr = TRUE,
  corr_text_size = 7,
  panel_border_color = "grey40",
  panel_border_size = 2,
  corr_digits = 2,
  show_mean_diag = TRUE,
  mean_line_color = "black",
  mean_linewidth = 0.7,
  mean_linetype = "dashed",
  output = c("draw", "ggplot")
)
}
\arguments{
\item{object}{A bootstrap result object of class
\code{"sbt_std_boot"} or \code{"sbt_ustd_boot"}.}

\item{params}{Character vector of parameter names to plot (≥ 2).}

\item{standardized}{Logical; whether to use standardized estimates.}

\item{title}{Plot title.}

\item{point_size, point_alpha, point_color}{Aesthetics for points in lower panels.}

\item{show_smooth, smooth_method, smooth_se}{Control the regression smoother in lower panels.}

\item{show_ellipse, ellipse_level, ellipse_color}{Confidence ellipse options in lower panels.}

\item{diag_type}{One of \code{"both"}, \code{"density"}, \code{"hist"}, \code{"blank"} for diagonal panels.}

\item{hist_border_size}{Histogram outline width in diagonal \code{"hist"}/\code{"both"}.}

\item{bins, dens_fill, dens_color, hist_fill, hist_color}{Diagonal panel styles.}

\item{show_corr}{Logical; if TRUE, show correlation coefficients in upper panels.}

\item{corr_text_size, corr_digits}{Text size and digits for upper-panel correlation.}

\item{panel_border_color, panel_border_size}{Panel border color and width for all panels.}

\item{show_mean_diag, mean_line_color, mean_linewidth, mean_linetype}{Draw a vertical mean line in diagonal panels.}

\item{output}{Either \code{"draw"} (print) or \code{"ggplot"} (return ggplot object).}
}
\value{
Invisibly returns the \code{ggplot} object (or prints it if \code{output="draw"}).
}
\description{
Generates a scatterplot matrix of the bootstrap estimates for
two or more model parameters using \code{ggplot2} and \code{GGally},
with optional smoothing lines, confidence ellipses, and
customizable density or histogram plots on the diagonal.
}
\details{
The function \code{\link[=gg_scatter_boot]{gg_scatter_boot()}} is an enhanced ggplot2-based
version of \code{\link[=scatter_boot]{scatter_boot()}}, designed to produce publication-quality
scatterplot matrices of bootstrap estimates.
It can be applied to the output of \code{\link[=standardizedSolution_boot]{standardizedSolution_boot()}}
or \code{\link[=parameterEstimates_boot]{parameterEstimates_boot()}}.
}
\seealso{
\code{\link[=scatter_boot]{scatter_boot()}}, \code{\link[=hist_qq_boot]{hist_qq_boot()}},
\code{\link[=standardizedSolution_boot]{standardizedSolution_boot()}}, \code{\link[=parameterEstimates_boot]{parameterEstimates_boot()}}
}
